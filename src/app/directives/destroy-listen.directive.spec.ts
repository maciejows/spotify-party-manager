import { DestroyListenDirective } from './destroy-listen.directive';

describe('DestroyListenDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroyListenDirective();
    expect(directive).toBeTruthy();
  });
});
