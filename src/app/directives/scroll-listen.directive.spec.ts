import { ScrollListenDirective } from './scroll-listen.directive';

describe('ScrollListenDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollListenDirective();
    expect(directive).toBeTruthy();
  });
});
